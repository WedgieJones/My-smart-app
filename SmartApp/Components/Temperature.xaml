<UserControl x:Class="SmartApp.Components.Temperature"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartApp.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top">
        <TextBlock Text="{Binding CurrentTempCondition}" FontFamily="{StaticResource FontAwesome-Regular}" FontSize="46" Margin="0 0 15 0" Foreground="white" />
        <TextBlock FontSize="22" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Center">
            <Run Text="{Binding CurrentTemperature}" FontSize="32" />
            <Run Text="°C" />
        </TextBlock>
    </StackPanel>
    
</UserControl>
